const API_BASE=(window.API_BASE||'').replace(/\/$/,'');
const DEMO_KEY='aae_v2_demo';
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));}
async function api(path,opt={}){const r=await fetch(API_BASE+path,opt);if(!r.ok)throw new Error((await r.json().catch(()=>({}))).error||'Erreur');return r.headers.get('content-type')?.includes('json')?r.json():r.text()}
function demoGet(id){const d=JSON.parse(localStorage.getItem(DEMO_KEY)||'{}');return d[id]}
function demoSave(x){const d=JSON.parse(localStorage.getItem(DEMO_KEY)||'{}');d[x.id]=x;localStorage.setItem(DEMO_KEY,JSON.stringify(d));}
function demoCreate(b){const id=(b.id||b.discipline.replace(/\W/g,'').toUpperCase().slice(0,8))+'-'+b.year+'-'+Math.random().toString(36).slice(2,6);const qs=[['Organisation générale','rating','organisation'],['Accueil et prise en charge','rating','accueil'],['Informations avant et pendant la compétition','rating','communication'],['Formule sportive','rating','formule'],['Déroulement et respect des horaires','rating','horaires'],['Installations et équipements','rating','installations'],['Hébergement','rating_na','hebergement'],['Restauration','rating_na','restauration'],['Recommanderiez-vous cette compétition ?','yesno','recommandation'],['Note globale de la compétition /10','overall','global'],['Votre principale piste d’amélioration','text','amelioration'],['Une remarque ou suggestion complémentaire ?','text','commentaire']].map((q,i)=>({id:i+1,label:q[0],type:q[1],domain:q[2],position:i+1}));(b.customQuestions||[]).forEach(x=>qs.push({id:qs.length+1,label:x,type:'rating',domain:'specifique',position:qs.length+1}));const c={id,year:b.year,discipline:b.discipline,name:b.name,organizer:b.organizer,questions:qs,responses:[]};demoSave(c);return c}
